apiVersion: v1
kind: Service
metadata:
  name: database-service-api
spec:
  type: ClusterIP
  selector:
    app: database-service-api
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
